In addition to the website insatructions for installing auto-sklearn I observed the following:

- Requires Python 3.9 environment. Easiest way to do this seems to be to `dnf install python3.9` then run `python3.9 -m venv ...`.
- Then pip install auto-sklearn.
- Then afterwards downgrade numpy to 1.26.1.

